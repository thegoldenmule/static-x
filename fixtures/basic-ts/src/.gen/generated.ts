// Stands in for framework-generated output (like Next.js .next/types):
// present in the program via tsconfig include, but not project-owned.
export type PageProps = { params: Promise<Record<string, string>> };
export const checkFields = (input: any): any => input!;
