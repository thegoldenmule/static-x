extends Node

## An autoload. project.godot is the only thing that references it, which
## is the case a dead-script analysis gets wrong if it reads .gd and .tscn
## files and stops there.

var score: int = 0
