// A duplicated pair below the default minNodes floor: never flagged
// with defaults, only when minNodes is lowered explicitly.
export function double(n: number): number {
  return n * 2;
}

export function twice(n: number): number {
  return n * 2;
}
