// Stands in for generated framework output (like Next.js .next/types):
// in the program via tsconfig include, hidden from analysis subjects,
// but its import must keep only-gen.ts alive.
import { onlyGen } from '../only-gen';

export const gen = onlyGen + 1;
