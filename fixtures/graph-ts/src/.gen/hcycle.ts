// Hidden member of the planted value cycle through a hidden directory
// (hid-a -> .gen/hcycle -> hid-a). Never a finding subject itself.
import { hidA } from '../hid-a';

export const hcycle = 1;
export const echo = (): number => hidA;
