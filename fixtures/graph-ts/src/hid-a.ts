// Non-hidden member of the value cycle through src/.gen/hcycle.ts; the
// cycle finding must anchor here, not on the generated file.
import { hcycle } from './.gen/hcycle';

export const hidA = hcycle + 1;
