// An extraRoots consumer whose namespace import names the analyzed
// package, which counts as consuming every file in the project.
import * as graphFixture from 'graph-fixture';

export const api = graphFixture;
